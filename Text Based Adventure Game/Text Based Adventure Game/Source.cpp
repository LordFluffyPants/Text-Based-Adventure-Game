#include <iostream>
#include <string>
#include <windows.h>
using namespace std;


//system("cls"); This will clear the command prompt
//system("PAUSE");standard press anykey to continue

int main(){
	ShowWindow(GetConsoleWindow(), SW_MAXIMIZE);
	system("mode 650");
	bool loop = true;
	string input; //Keeping this variable will store the last user input

	while(loop){
		cout<< "WELCOME TO name pending game" <<endl;
		system("PAUSE");
		
		


		
	}
	return 0;
}